// 7.Problem Statement:
// Write a program that takes two integers as input and prints the absolute difference between them.
// If the first number is greater than the second, print the result as is
// If the second number is greater than the first, also print the positive difference.
// 7.

let a = 7; b = 9;
if(a > b){
    console.log(a-b);
}else{
    console.log(b-a);
}